#include <stdio.h>
/**
*main - prints a message using hte function puts that outputs plain text
*
*instead of printf.
*
* Return: a 0 value confirming the correct run of the code
*/
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
