# 0x0a. C - malloc, free

In this project, I learned about dynamic memory allocation in C. I practiced using `malloc` to allocate memory dynamically and `free` to release it, while ensuring I avoid memory leaks.

## Tasks
* **0-create_array.c**: A function that creates an array of chars, and initializes it with a specific char.
